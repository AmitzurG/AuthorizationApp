<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent" android:layout_height="match_parent"
    tools:context=".view.SplashFragment" tools:viewBindingIgnore="true" >

    <ImageView
        android:id="@+id/appImageView"
        android:layout_width="@dimen/splashImageSize" android:layout_height="@dimen/splashImageSize"
        android:src="@mipmap/ic_launcher_round" android:scaleType="fitCenter"
        android:contentDescription="@string/appImage"
        app:layout_constraintBottom_toTopOf="@id/appNameTextView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/appNameTextView"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:text="@string/appName" android:textAppearance="?android:attr/textAppearanceLarge"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/appImageView" />
</androidx.constraintlayout.widget.ConstraintLayout>